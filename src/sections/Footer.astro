---
import LogoPagina from "@components/LogoPagina.astro"
import {carreras} from "@data/carreras.json"

const NombreCarreras = carreras.map(({carrera}) => carrera.replace(/-/g, " "))
---

<section
	class="bg-surface-variant text-on-surface-variant rounded-3xl p-6 flex flex-col sm:flex-row gap-2.5 items-start justify-between self-stretch shrink-0 relative overflow-hidden">
	<div class="flex flex-col gap-2.5 items-start w-full sm:w-auto flex-1 justify-start shrink relative">
		<LogoPagina />
		<p class="text-left text-label-large relative flex items-start justify-start w-full">
			Este sitio no es una página oficial de FaCENA. Está diseñado para brindar información de manera más accesible y
			organizada. Si encontrás algún error, como fechas incorrectas, cambios en los materiales o cualquier otra
			información desactualizada, no dudes en contactarme para corregirlo. ¡Tu ayuda es muy valiosa para mantener esta
			página actualizada y útil para todos!
		</p>
		<figure class="flex flex-col gap-[5px] items-center justify-center shrink-0 w-[121px] relative">
			<img class="w-min h-min relative overflow-visible" src="/MiLogo.svg" />
			<figcaption class="text-center text-xs font-bold relative self-stretch flex items-center justify-center">
				By MatiDevCode
			</figcaption>
		</figure>
	</div>
	<div class="flex flex-col gap-3 items-start w-auto justify-start shrink-0 relative">
		<h2 class="text-left text-title-large relative self-stretch flex items-center justify-start">Carreras:</h2>
		<div class="flex flex-col gap-2.5 items-start justify-start shrink-0 relative">
			{
				NombreCarreras.map((carrera) => (
					<p class="text-left text-title-small relative flex items-center justify-start">{carrera}</p>
				))
			}
		</div>
	</div>
</section>