---
import LogoPagina from "@components/LogoPagina.astro";
import { carreras } from "@data/carreras.json";
import { Icon } from "astro-icon/components";
import MiLogo from "../components/MiLogo.astro";

const enlacesCarreras = carreras.map(({ carrera }) => ({
	href: `/carreras/${carrera}`, // Enlace con guiones
	nombre: carrera.replace(/-/g, " "), // Texto sin guiones
}));

const contactos = [
	{
		label: "Github",
		link: "https://github.com/MatiasSolisSchneeberger",
		icono: "brand-github",
	},
	{
		label: "Matias.Solis.Sch@gmail.com",
		icono: "mail",
	},
	{
		label: "Linkedin",
		link: "https://www.linkedin.com/in/matias-solis-schneeberger-40a6842ab/",
		icono: "brand-linkedin",
	},
	{
		label: "Repositorio",
		link: "https://github.com/MatiasSolisSchneeberger/the-swifties-eras-tour",
		icono: "brand-github-filled",
	},
];
---

<section
	class="bg-surface-variant text-on-surface-variant rounded-3xl p-6 flex flex-col sm:flex-row gap-2.5 items-start justify-between self-stretch shrink-0 relative overflow-hidden">
	<article
		class="flex flex-col gap-2.5 items-start w-full sm:w-auto flex-1 justify-start shrink relative">
		<LogoPagina />
		<p
			class="text-left text-label-large relative flex text-pretty items-start justify-start w-full">
			Este sitio no es una página oficial de FaCENA. Está diseñado para
			brindar información de manera más accesible y organizada. Si
			encontrás algún error, como fechas incorrectas, cambios en los
			materiales o cualquier otra información desactualizada, no dudes
			en contactarme para corregirlo. ¡Tu ayuda es muy valiosa para
			mantener esta página actualizada y útil para todos!
		</p>
		<aside
			class="w-full flex flex-col items-start gap-2 overflow-hidden sm:gap-4 lg:w-1/3">
			<h4 class="text-title-medium mb-2">Contactos</h4>
			<div>
				{
					contactos.map(({ label, link }) => {
						return link ? (
							<a
								href={link}
								class="items flex flex-row gap-2 transition-all duration-100 ease-in-out hover:underline hover:opacity-90">
								{/* {icono && <Icon name={icono} size={22} class="hidden sm:inline"/>} */}
								<span class="">
									{label}
								</span>
							</a>
						) : (
							<span
								id="copiarCorreo"
								class="items flex flex-row gap-2 transition-all duration-100 ease-in-out hover:underline hover:opacity-90">
								{/* {icono && <Icon name={icono} size={22} class="hidden sm:inline"/>} */}
								<span class="">
									{label}
								</span>
							</span>
						);
					})
				}
			</div>
		</aside>
		<div class="h-10 justify-start">
			<MiLogo color="on-primary-container" />
		</div>
	</article>
	<article
		class="flex flex-col gap-3 items-start w-auto justify-start shrink-0 relative">
		<h2
			class="hidden text-left text-title-large relative self-stretch sm:flex items-center justify-start">
			Carreras:
		</h2>
		<div
			class="hidden sm:flex flex-col gap-2.5 items-start justify-start shrink-0 relative">
			{
				enlacesCarreras.map(({ href, nombre }) => (
					<a
						href={href}
						class="text-left text-title-small underline relative flex items-center justify-start">
						{nombre}
					</a>
				))
			}
		</div>
	</article>
</section>
